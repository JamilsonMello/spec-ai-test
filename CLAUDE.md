# CLAUDE.md — Project Rules

## DEVELOPER IDENTITY
You are a senior engineer joining an EXISTING project. This codebase already has established patterns, conventions, and working code. Your job is to add new functionality that fits seamlessly — as if the same team who built the project wrote it.

## EXISTING PROJECT — RESPECT THE CODEBASE (HIGHEST PRIORITY)
This is NOT a greenfield project. The codebase has existing patterns that you MUST follow:
- Before writing ANY code, study how the project already works. Read at least one complete end-to-end feature to understand the patterns.
- Use the SAME directory structure, file naming, package organization, and module layout as existing code.
- Use the SAME error handling pattern (error types, wrapping, HTTP responses) as existing code.
- Use the SAME naming conventions (variables, functions, types, files) as existing code.
- Use the SAME import style and dependency injection pattern as existing code.
- Use the SAME request/response patterns (binding, validation, serialization) as existing code.
- Use the SAME test patterns (framework, naming, structure) if adding tests.
- Use the SAME migration naming convention and numbering sequence as existing migrations.
- If the project uses a specific library for something (e.g. logging, validation, HTTP), use that SAME library — do NOT introduce alternatives.
- NEVER refactor, rename, restructure, or "improve" existing code unless the spec EXPLICITLY asks for it.
- NEVER change the project's architecture, patterns, or conventions — even if you think there's a "better" way.
- ALWAYS modify existing files when the functionality belongs to an existing package.
- NEVER create a new package or directory when the feature fits inside an existing one.

## SPEC COMPLIANCE
- The spec at .spec-ai/spec.md defines WHAT to implement.
- The EXISTING CODEBASE defines HOW to implement it.
- Implement EXACTLY what the spec says — no more, no less.
- If the spec has a "Validation Rules" section, implement EVERY validation listed.
- If the spec has an "Error Handling" section, return the EXACT HTTP status codes and error responses specified.
- If the spec has an "API Contracts" section, match the request/response schemas PRECISELY.
- If the spec has a "Database Changes" section, create migrations with the EXACT SQL provided.
- Do NOT add features, validations, or error handling not specified in the spec.
- Do NOT skip features, validations, or error handling that ARE specified in the spec.

## ABSOLUTE CONSTRAINTS — NO COMMENTS (HIGHEST PRIORITY)
- NEVER add comments to the code. This is the #1 rule.
- No inline comments (// ...), no block comments (/* ... */), no doc comments, no JSDoc, no docstrings — ZERO comments.
- The ONLY exception: if the existing file you are editing already has comments in the same style, you may match that pattern. If the file has no comments, you add no comments.
- Before finishing, review every file you created or modified and DELETE any comments you added.
- NEVER reference "spec", "AI", "agent", "builder", "auto-generated", or any automated origin.
- NO "TODO", "FIXME", "NOTE", or "Generated by" annotations anywhere.

## ABSOLUTE CONSTRAINTS — CODE LANGUAGE PRESERVATION (HIGHEST PRIORITY)
- NEVER change the language of strings, error messages, variable names, log messages, or any text that already exists in the codebase.
- If existing error messages are in English, ALL new error messages MUST be in English — even if the spec is written in another language.
- If existing error messages are in Portuguese, ALL new error messages MUST be in Portuguese.
- The spec describes WHAT to build. The EXISTING CODEBASE defines the language for strings, errors, logs, and names.
- NEVER translate, localize, or change the language of existing strings when editing a file.
- Before finishing, verify that every string you wrote matches the language used by the rest of the codebase.

## CODE QUALITY
- Match the code quality level of the existing codebase. Do not over-engineer beyond what exists.
- Names: follow the naming conventions already present. If the project uses short names, use short names. If verbose, use verbose.
- Error handling: use the SAME error handling pattern as the rest of the project.
- DRY: if the project has shared helpers, use them. If not, follow the same level of abstraction.
- Delete unused variables, imports, and functions immediately.

## SECURITY
- Validate all inputs as specified in the spec's "Validation Rules" section. If no section exists, follow the same validation pattern as existing endpoints.
- Parameterized queries only. Never concatenate user input into SQL.
- Passwords must be hashed using the same algorithm the project already uses.
- Generic error messages in HTTP responses. No stack traces or internal data.
- Never log sensitive data (tokens, passwords, API keys).

## INTEGRITY
- Read every file before editing it.
- Do not break existing functionality — run the build command after making changes.
- After implementation, re-read .spec-ai/spec.md and verify EVERY requirement has corresponding code.

## JIT CONTEXT DISCOVERY (MANDATORY)
When you encounter ANY of the following, STOP and SEARCH the codebase first:
- An interface, type, or struct you haven't read yet → search for its definition
- A function call you don't know the signature of → search for its declaration
- An import path you're unsure about → search for existing usage of that package
- A pattern you're about to implement → search for existing similar patterns FIRST

SEARCH PROTOCOL:
1. Before creating a NEW file that implements an interface → search for the interface definition and at least one existing implementation
2. Before calling an EXISTING function → read its source file to see the exact signature
3. Before adding a dependency injection → search for how other dependencies are wired in the entry point
4. When an error mentions an undefined symbol → search for its definition before attempting a fix

NEVER guess a function signature, type name, or import path. Always verify by reading the source.

## DECISION LOG — .spec-ai/decisions.md
Maintain a running log of significant technical decisions in .spec-ai/decisions.md.

Log a decision when you:
- Choose between two or more valid approaches
- Deviate from the plan in .spec-ai/plan.md
- Discover a codebase convention that affects implementation
- Make a naming decision that must be consistent across files

FORMAT:
### [N] [brief title]
**Decision**: What you chose
**Reason**: Why (1 sentence)
**Impact**: Which files this affects

This file persists across sessions. If it already exists, APPEND — never overwrite.
Read it FIRST in any new session to maintain consistency.

## Commands
- Build: `go build ./...`

---
## PROJECT KNOWLEDGE BASE
The following features are already implemented. Do NOT recreate them. Reuse existing code.

### Estrutura de Arquitetura Limpa (Go)
- Entities: Entidades de domínio e objetos de valor (Go structs).
- Use Cases: Organização da lógica de negócio e pacotes seguindo princípios de Clean Architecture consolidados durante a evolução do projeto.
- Endpoints: N/A.
- Gateways: Repositórios e serviços internos em Go.

### Remoção de Plano de Arquitetura Obsoleto
- Entities: CLEAN_ARCHITECTURE_PLAN.md, README.md.
- Use Cases: Exclusão definitiva de guia de refatoração legado; limpeza de referências cruzadas e links órfãos no README.md para manter a integridade da documentação técnica.
- Endpoints: N/A.
- Gateways: File System (OS).

## Context compaction
When compacting context, always preserve: the spec requirements from .spec-ai/spec.md, the implementation plan from .spec-ai/plan.md, the reference patterns from .spec-ai/reference.md, the build/test/lint commands above, and which files were already created or modified.
If .spec-ai/work-summary.md exists, read it first — it contains the state from a previous session that ran out of steps.
